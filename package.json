{
  "name": "rigyard-playcanvas",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "scripts": {
    "prebuild": "node scripts/fetch-assets.mjs",
    "build": "vite build",
    "dev": "vite --host 0.0.0.0",
    "preview": "vite preview --host 127.0.0.1 --port 4173",
    "test": "node tests/static.mjs",
    "test:e2e": "playwright test"
  },
  "dependencies": {
    "playcanvas": "2.22.2",
    "sync-ammo": "0.1.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "vite": "^8.2.0"
  }
}
